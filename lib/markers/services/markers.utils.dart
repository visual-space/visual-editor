import '../../documents/models/attributes/marker.model.dart';
import '../../documents/models/document.model.dart';

class MarkersUtils {
  // Returns a list of all the markers that have been defined in the document.
  // Provides also the pixel coordinates of each marker.
  static List<MarkerM> getAllMarkersFromDoc(DocumentM document) {
    final List<MarkerM> markers = [];

    return markers;
  }
}
