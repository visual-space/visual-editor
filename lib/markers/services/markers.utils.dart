// import '../models/marker.model.dart';
// import '../../documents/models/document.model.dart';

class MarkersUtils {

  // TODO On hold, no longer required as originally expected. However we might need this in the future.
  // We are now collecting this information at runtime when building the editor such that we can also retrieve the pixel coordinates.
  // ---
  // Returns a list of all the markers that have been defined in any delta document.
  // Can't provide the pixel coordinates, these are available only after rendering.
  // static List<MarkerM> getAllMarkersFromDoc(DocumentM document) {
  //   final List<MarkerM> markers = [];
  //
  //   return markers;
  // }
}
