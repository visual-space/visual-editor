import 'package:flutter/material.dart';

// The objects supplied to the CompositedTransformTarget objects that wrap the text field and selection handles.
class SelectionLayersState {
  final toolbarLayerLink = LayerLink();
  final startHandleLayerLink = LayerLink();
  final endHandleLayerLink = LayerLink();
}
